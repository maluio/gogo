{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <tbody>
            {% for item in items %}
            <tr>
                <td>
                    {{ item.question }}
                </td>
                <td>
                    {{ item.answer }}
                </td>
                <td>
                    {{ item.dueAt|date('Y-M-D H:i:s') }}
                </td>
                <td>
                    <a href="{{ path('item_edit', {'id': item.id}) }}">Edit</a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}