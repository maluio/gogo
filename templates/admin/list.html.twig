{% extends 'base.html.twig' %}

{% block body %}
    <table class="table">
        <tbody>
            {% for item in items %}
            <tr>
                <td>
                    {{ item.question|nl2br }}
                </td>
                <td>
                    {{ item.answer|nl2br }}
                </td>
                <td>
                    {{ item.dueAt|due_since }}
                </td>
                <td>
                    <a href="{{ path('item_edit', {'id': item.id}) }}">Edit</a>
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}