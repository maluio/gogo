version: '3'

services:
  db:
    image: mysql:5.7
    restart: always
    expose:
      - 3306
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=gogo
      - MYSQL_PASSWORD=gogo
      - MYSQL_USER=gogo
    networks:
      - private